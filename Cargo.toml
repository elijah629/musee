[package]
name = "musee"
version = "0.1.0"
edition = "2024"
description = "Music library organizer and repair CLI"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
metaflac = "0.2"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
